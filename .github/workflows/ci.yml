name: CI — PR Gate

on:
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  commit-trailer:
    name: Commit Trailer Check
    uses: ./.github/workflows/reusable-commit-trailer.yml
