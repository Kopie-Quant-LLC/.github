name: CI — PR Gate

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  commit-trailer:
    name: Commit Trailer Check
    uses: ./.github/workflows/reusable-commit-trailer.yml
